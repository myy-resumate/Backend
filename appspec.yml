#CodeDeploy Agent가 배포할 때 실행하는 스크립트
version: 0.0
os: ubuntu
files:
  - source: /
    destination: /home/ubuntu/resumate-deploy/  #프로젝트 이름
    overwrite: yes

permissions:
  - object: /
    pattern: "**"
    owner: ubuntu
    group: ubuntu